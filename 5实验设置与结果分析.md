## 5 实验设置与结果分析

如第 4 章 Table 2 所示，现有公开评测基准在样本单位、标签对象和数据透明度上与连续浏览场景并不同构；第 3 章则进一步把连续页面访问、网站级标签空间和多模态辅助信息落实为新的数据前提。本章将前述结构性失配转化为三项可定量检验的实证问题：首页孤立特征假设会系统性抬高目标场景下的性能估计，公开评测基准上的高分结论需要按封装环境对齐后重新检验，候选网站标签空间扩展会持续重排默认参数基线下的迁移表现。

围绕这三项判断，本章组织三组实验。`5.2` 是用于证伪“首页流量 = 网站流量”默认假设的定向证伪实验；`5.3` 在同一网络封装协议阵营内部进行综合对照；`5.4` 把 Top-K 任务重新界定为宏观时间窗下的锚点特征提取与匹配。三组实验共同构成从样本单位、封装环境到标签规模的前提重构证据链。

### 5.1 实验目标与统一设置

本章中的实验对象分为四类。五个开源数据集承担公开任务参照身份，用于刻画现有文献最常见的公开评测边界；TeraWFD_VPN 与 TeraWFD_NOVPN 是统一网站标签组织下、在同一客户端主机侧容器测量视角下按不同网络封装环境切出的自建统计子集；Top-K 前缀标签集合是在 TeraWFD 内部定义的候选网站标签规模控制任务；`5.2` 中的 100 网站集合则是固定标签集下的定向证伪子任务，只改变训练样本组织方式而不改变标签定义。

本章检验主流网站指纹识别模型在连续宏观时间窗下的有效表征能力。TeraWFD 中一个网站标签对应由主页面、站内页面生命周期和页面脚本共同触发的长时访问过程。模型面对包含并行请求、第三方跨域资源、周期性心跳和页面内生后台流的完整观测窗口，因此本章重点观察模型在全量观测前提下的有效样本级判别能力。

SiteBench 在本章采用严格的全量流归属原则（Full-trace Flow Attribution）。对于每一个宏观浏览轨迹 $S_y$，由主页面、站内页面生命周期或页面脚本触发的所有子流，均继承该轨迹的唯一主站标签 $y$。预处理阶段按各基线模型原论文或开源实现对网络输入的要求进行必要预处理。数据划分采用分层随机抽样（Stratified Random Split）。在每个实验定义的全量有效样本集合内，本文按网站标签分层，将样本以 8:1:1 的比例划分为训练集、验证集和测试集。为确保划分后每个网站类别在验证集和测试集中均具备最小有效支撑（Minimum Support），避免极端尾部标签出现零样本（Zero-shot）的评估分布，本文剔除了在预处理后有效样本总数不足 10 个的极小类别。该策略用于稳定极端类别不平衡条件下各类别在三类集合中的分布，并重点评估模型在样本单位一致条件下面对海量并发流、长周期页面内生后台流和共享资源干扰时维持稳定判别边界的能力。

本文统一采用 DF、AppScanner、FS-Net 和 YaTC 四个基准模型。四者分别代表卷积时序模型、统计特征模型、循环时序模型和基于 Transformer 的字节级模型路线。DF 只保留报文方向序列，AppScanner 使用 54 维流量统计特征并采用 100 棵树的随机森林分类器，FS-Net 使用带方向的报文长度序列，YaTC 则把前 5 个包的字节内容重塑为图像输入。模型集合、选择规则和评价指标在不同实验块之间保持一致。

在超参数配置方面，本文的核心目标是评估现有 WFP 基线模型在开箱即用（Out-of-the-box）条件下的系统鲁棒性和部署可承受性。WFP 模型既是数学映射函数，也是受计算开销、内存占用、推理延迟和在线部署预算约束的物理系统。若为了适配连续宏观时间窗而将现有模型的输入截断长度扩大数十倍，卷积、循环和注意力模块的 FLOPs、显存占用和端到端推理延迟都会随输入规模显著增长；对全局注意力模型而言，序列长度增长还会带来更陡峭的计算代价。这类目标域重度定制可能挽回部分指标，却会改变原模型面向轻量级流量分析的部署约束。基于这一系统约束，本文主动冻结各基线模型的初始超参数空间，统一沿用其原论文或开源实现中推荐的默认配置（详见 Table 6）。该受控设置用于暴露传统单页面特征提取机制直接进入长周期流量时的稳定性边界，并为下一代低开销、抗干扰特征表征提供基准。

Table 6 给出了与可复现性和统一评估口径直接相关的实验设置。神经网络模型统一采用验证集指标最高的检查点作为最终测试模型，AppScanner 在训练集拟合后直接在验证集和测试集评估。表中保留影响受控对比的主干配置。

Table 6 4 个基准模型的统一实验口径

| 模型 | 代表路线 | 主要输入表示 | 关键训练配置 | 模型选择规则 |
|---|---|---|---|---|
| DF | 卷积时序模型 | 报文方向序列（±1，丢弃长度） | 遵循原论文或开源实现推荐配置 | 验证集指标最高检查点 |
| AppScanner | 统计特征模型 | 54 维流量统计特征 | 100 棵树随机森林分类器 | 训练集拟合后评估 |
| FS-Net | 循环时序模型 | 带方向的报文长度序列 | 遵循原论文或开源实现推荐配置 | 验证集指标最高检查点 |
| YaTC | 字节级 Transformer 模型 | 前 5 个包字节重塑的 40×40 图像 | 遵循原论文或开源实现推荐配置 | 验证集指标最高检查点 |

这组输入表示差异为后文的默认机制鲁棒性差异提供了必要背景。DF 的 direction-only 表示在高信噪比单页面任务中仍可捕捉访问模式，但进入连续浏览宏观轨迹后，页面内生后台流、并行请求和页面间复用会迅速稀释纯方向模式；FS-Net 保留方向与长度联合信息，因此在 TeraWFD_VPN 和 TeraWFD_NOVPN 的综合对照中维持了更优的判别下限；YaTC 依赖早期包字节图像，在公开数据集上表现强，当前 5 个包与目标页面主导航事件之间的对应关系在连续浏览场景中更不稳定，其 Macro-F1 会更早暴露类别层面的失衡。

本文统一报告 Accuracy 与 Macro-F1，且所有指标均在有效样本级计算。设当前 Top-K 分类任务的有效测试样本集合为 $D_K = \{(x_i, y_i)\}_{i=1}^{N_K}$，其中 $x_i$ 是经底层协议清洗与结构对齐后实际送入分类器的有效网络流样本，$y_i$ 是继承自宏观轨迹的主站标签。模型对每个 $x_i$ 独立输出在 $K$ 个受控网站标签上的概率分布，仅当 $\arg\max f(x_i) = y_i$ 时，该有效样本记为一次正确预测。

$$Accuracy = \frac{1}{N_K} \sum_{i=1}^{N_K} \mathbb{I}[\arg\max f(x_i) = y_i]$$

$$Macro\text{-}F1 = \frac{1}{|C_K|} \sum_{c \in C_K} F1_c$$

其中，$N_K$ 为实际进入模型的有效测试样本总数，$C_K$ 为当前控制的网站标签集合。本文采用有效样本级计分：每个有效网络流样本独立参与判断，轨迹级 Max Pooling、“一次命中即全局正确”以及宏观轨迹内任一片段命中目标标签即判定整条轨迹正确的聚合口径均不进入指标计算。这样的有效样本级计分方式更严苛，但能直接暴露模型在充斥页面内生后台流、共享资源和并行请求的连续时间窗内是否仍能维持稳定精确率。

Accuracy 在本文中用于与既有文献维持后向兼容，Macro-F1 用于刻画宏观时间窗任务下类别层面的平均识别能力。对于头部易识别站点与海量中长尾站点并存的 TeraWFD 而言，Accuracy 容易触发“准确率悖论”，使局部头部类别带来的表面命中率掩盖大量网站已经失去稳定判别力的事实。后文关于网站级识别稳定性的判断，将以 Macro-F1 作为核心判据，Accuracy 作为与历史基准对齐的辅助参照。第五章结果也体现了这种指标分工：在 TeraWFD_NOVPN 的综合对照中，最高 Accuracy 来自 FS-Net（0.4334），最高 Macro-F1 则来自 AppScanner（0.3085）。前者反映总体有效样本的命中率，后者揭示长尾类别层面的平均识别下限；二者的差异本身就是长尾网站级评测需要同时报告两类指标的原因。

### 5.2 评测前提证伪：从首页孤立特征到连续会话的分布偏移

`5.2` 是一场定向证伪实验（Targeted Falsification Experiment）。实验在同一网站集合和标签定义下，依次比较仅首页训练、全站单页面训练和连续会话同分布训练三种样本组织方式，用验证集与测试集之间的差距衡量“首页流量 = 网站流量”这一默认假设的稳定性。若训练样本与目标场景不同构，验证集对测试集的估计会出现明显断裂；若连续页面访问样本更接近网站级连续浏览任务，验证集与测试集差距应显著收缩。

本节将候选网站集合控制在 100 个网站的受控子任务内，以固定标签规模并隔离样本单位变量。若直接在 5,000 余网站规模下开展同类证伪，性能变化将同时受到连续会话时间维度和候选标签空间急剧膨胀的影响。将规模锁定在 100，可以先剥离规模变量，观察单页面样本切换到连续浏览样本后对模型表征判断的影响。随后 `5.3` 与 `5.4` 再释放封装环境和标签规模变量，展示中心问题被重新审视后的结果权重变化。

实验固定网站集合、标签定义和数据划分方式，只改变训练样本的组织方式。数据集 B 将同一网站的 10 个目标 URL 分别独立访问并保存，其中包含 1 个首页与 9 个同站子页面；数据集 A 将同一组 10 个 URL 按连续页面访问方式组织为 1 个会话样本。实验一只使用数据集 B 中的首页样本完成训练和验证，测试阶段引入同站子页面与连续页面访问样本；实验二将训练数据扩展为数据集 B 中的首页与全部同站子页面，验证集仍来自单页面样本，测试集继续使用数据集 A 中的连续页面访问样本；实验三直接使用数据集 A 中的连续页面访问样本完成训练、验证与测试，提供目标场景下的同分布参照线。

Table 7 给出了三组定向证伪实验的训练、验证与测试结果。AppScanner 的输出不包含神经网络训练轮次形式的训练集准确率，表中对应训练集位置保留为空。

Table 7 评测前提证伪实验中的训练集、验证集与测试集结果

| 实验设置 | 数据划分 | DF（Acc / Macro-F1） | AppScanner（Acc / Macro-F1） | FS-Net（Acc / Macro-F1） | YaTC（Acc / Macro-F1） |
|---|---|---|---|---|---|
| 仅首页训练 | 训练集 | 0.8429 / - | - / - | 0.8774 / - | 0.6908 / - |
| 仅首页训练 | 验证集 | 0.5428 / 0.5974 | 0.5830 / 0.5976 | 0.7891 / 0.8832 | 0.4368 / 0.5667 |
| 仅首页训练 | 测试集 | 0.3611 / 0.3594 | 0.3530 / 0.3391 | 0.4419 / 0.5101 | 0.2226 / 0.3615 |
| 全站单页面训练 | 训练集 | 0.7819 / - | - / - | 0.7051 / - | 0.4853 / - |
| 全站单页面训练 | 验证集 | 0.6608 / 0.7141 | 0.5641 / 0.5743 | 0.7013 / 0.7902 | 0.4088 / 0.5802 |
| 全站单页面训练 | 测试集 | 0.2977 / 0.3459 | 0.2738 / 0.2836 | 0.3807 / 0.4987 | 0.2958 / 0.4518 |
| 连续会话训练 | 训练集 | 0.7539 / - | - / - | 0.6517 / - | 0.9895 / - |
| 连续会话训练 | 验证集 | 0.7729 / 0.8135 | 0.5320 / 0.5495 | 0.6872 / 0.7995 | 0.9824 / 0.9575 |
| 连续会话训练 | 测试集 | 0.7491 / 0.7947 | 0.5174 / 0.5335 | 0.6735 / 0.7921 | 0.9876 / 0.9633 |

仅首页训练时，验证集与测试集之间出现明显断裂。DF 的 Accuracy 从验证集的 0.5428 降至测试集的 0.3611，Macro-F1 从 0.5974 降至 0.3594；FS-Net 的 Accuracy 从 0.7891 降至 0.4419，Macro-F1 从 0.8832 降至 0.5101；YaTC 的 Accuracy 从 0.4368 降至 0.2226，Macro-F1 从 0.5667 降至 0.3615。验证集与测试集的显著断裂说明，只依赖首页孤立特征得到的判别边界无法稳定迁移到同站子页面和连续会话样本。

全站单页面训练扩大了页面覆盖范围，却仍然没有消除目标场景失配带来的分布偏移。DF 在验证集上达到 0.6608 Accuracy 和 0.7141 Macro-F1，测试集却跌至 0.2977 和 0.3459；FS-Net 从 0.7013 / 0.7902 降至 0.3807 / 0.4987；AppScanner 从 0.5641 / 0.5743 降至 0.2738 / 0.2836。同站子页面纳入训练后，模型在单页面验证集上形成的判别规则仍然无法稳定迁移到连续页面访问测试集。这说明“单页面独立采样”与“连续页面访问样本”之间存在无法忽视的样本单位断层。

这一组结果还有一个反直觉信号：全站单页面训练相对仅首页训练未形成稳定增益，3 个模型的测试 Accuracy 进一步下降。DF 从仅首页训练时的 0.3611 降至 0.2977，FS-Net 从 0.4419 降至 0.3807，AppScanner 从 0.3530 降至 0.2738。扩展同站页面覆盖不会自然逼近连续浏览任务；当训练和验证仍被组织为彼此独立的单页面样本时，更多页面还可能让模型在单页面验证集上呈现更高置信度的过拟合误判。

连续会话训练提供了目标场景下的同分布参照。DF 的验证集与测试集 Accuracy 分别为 0.7729 和 0.7491，Macro-F1 分别为 0.8135 和 0.7947；FS-Net 分别为 0.6872 和 0.6735，Macro-F1 分别为 0.7995 和 0.7921；YaTC 的验证集与测试集保持在 0.9824 / 0.9575 与 0.9876 / 0.9633 的相近水平。与前两组实验相比，验证集与测试集差距明显收缩，表明训练样本单位与目标场景趋于同构。

若把四个模型的验证集与测试集断裂取均值，这一趋势更加清楚。仅首页训练下，平均 Accuracy gap 约为 0.243，平均 Macro-F1 gap 约为 0.269；全站单页面训练下，二者仍约为 0.272 和 0.270。连续会话训练后，平均 Accuracy gap 收缩到约 0.012，平均 Macro-F1 gap 收缩到约 0.009。核心证据在于验证集对测试集的估计重新变得可信。连续页面访问样本把训练、验证和测试放回同一种物理生成过程后，默认参数基线下的迁移表现不再受到样本单位失配的系统性扭曲。

本组实验通过严格的控制变量，在宏观尺度上确证了单页面前提迁移到连续浏览场景时存在系统性的分布偏移，并由此引发判别边界迁移失效。前端 DOM 缓存策略、HTTP/2 或 HTTP/3 多路复用、连接复用、浏览器调度和跨页面资源共享分别贡献了多少干扰权重，需要依托更细粒度的后续归因实验继续分解。SiteBench 同步保留 DOM、截图、TLS 会话密钥和访问元信息，正是为了把这类宏观失配转化为后续微观跨页面物理特征归因（Microscopic Feature Attribution）的可计算数据底座。

本组结果表明，传统的单页面评测范式无法有效近似连续浏览场景下的模型表现。首页样本和全站单页面样本均不能提供与目标场景一致的样本定义；只有将连续页面访问同时作为训练、验证和测试的基本样本单位，验证集对测试集的估计才恢复稳定，并更接近目标场景的分布前提。

### 5.3 综合对照：公开任务结论能否外推

综合对照实验按网络封装协议分区，检验公开评测基准上的高分结论能否外推到 TeraWFD 的网站级连续浏览前提。五个开源数据集保留各自原生任务边界，代表现有文献最常见的公开评测环境；TeraWFD_VPN 与 TeraWFD_NOVPN 作为统一标签组织下、在同一客户端主机侧容器测量视角下按封装环境切出的自建统计子集，用于观察同一组模型进入网站级任务后结果如何变化。

为在统一的数据口径下评估各模型的基线迁移表现，并严格剥离类别规模这一干扰变量，Table 8 中采用的 TeraWFD_NOVPN 与 TeraWFD_VPN 分别抽样并对齐到 100 个目标网站，使其与表内公开任务保持相近的类别规模。在标签空间与网络封装前提同时受控的条件下，TeraWFD 统计子集与公开任务之间仍呈现显著分数量级落差，说明结果差异来自连续浏览宏观时间窗下的结构性识别难度上升。

`5.3` 在每个数据集内部采用训练集训练、测试集测试的 IID 口径。TeraWFD 上的整体分数水平对应更高的结构性识别难度。具体而言，当评测前提从高信噪比的“静态网页快照”切换到包含页面生命周期内生后台流、串行连续访问中的共享资源重叠和并行资源加载的“宏观轨迹态”后，样本内部的目标指纹 / 非目标干扰比显著恶化，特征熵随之上升，WFP 任务也从相对直接的模式识别演变为复杂的抗干扰特征提取。

Table 8 按封装环境拆成两个独立区块。区块 A 放置直连 / 非隧道环境任务与 TeraWFD_NOVPN，区块 B 放置隧道 / 混淆封装环境任务与 TeraWFD_VPN。区块 B 内部同时包含 VPN 隧道与 Tor 强混淆代理，作为通向 TeraWFD_VPN 的同阵营参照；TeraWFD_VPN 对应 Trojan TLS 外观隧道封装，是代表性的 TLS 封装隧道单协议 case study。

Table 8 公开任务与 TeraWFD 自建统计子集在 100 类对齐条件下按封装环境分区的综合对照结果

区块 A 直连 / 非隧道环境任务与 TeraWFD_NOVPN

| 数据集 | DF（Acc / Macro-F1） | AppScanner（Acc / Macro-F1） | FS-Net（Acc / Macro-F1） | YaTC（Acc / Macro-F1） |
|---|---|---|---|---|
| USTC-TFC | 0.8250 / 0.6331 | 0.8246 / 0.7935 | 0.8238 / 0.8587 | 0.9852 / 0.9860 |
| Cross-Platform | 0.1070 / 0.0699 | 0.4828 / 0.4656 | 0.5696 / 0.2791 | 0.8423 / 0.7480 |
| CIC-IoT-2022 | 0.7983 / 0.3251 | 0.9776 / 0.9732 | 0.9587 / 0.6999 | 0.9993 / 0.9722 |
| TeraWFD_NOVPN | 0.0792 / 0.0290 | 0.2812 / 0.3085 | 0.4334 / 0.2841 | 0.2998 / 0.1875 |

区块 B 隧道 / 混淆封装环境任务与 TeraWFD_VPN

| 数据集 | DF（Acc / Macro-F1） | AppScanner（Acc / Macro-F1） | FS-Net（Acc / Macro-F1） | YaTC（Acc / Macro-F1） |
|---|---|---|---|---|
| ISCX-VPN | 0.5909 / 0.4266 | 0.7752 / 0.7749 | 0.7365 / 0.7186 | 0.6775 / 0.8341 |
| ISCX-Tor | 0.9227 / 0.5117 | 0.9564 / 0.9529 | 0.9694 / 0.5591 | 0.9820 / 0.6934 |
| TeraWFD_VPN | 0.1564 / 0.0548 | 0.3135 / 0.3077 | 0.6816 / 0.4462 | 0.3986 / 0.1234 |

在区块 A 内，传统非隧道公开任务形成较高参照线。YaTC 在 USTC-TFC 和 CIC-IoT-2022 等特定域任务上表现出极高拟合能力，Accuracy 分别达到 0.9852 和 0.9993，Cross-Platform 上也达到 0.8423；AppScanner 在 USTC-TFC 与 CIC-IoT-2022 上的 Macro-F1 分别达到 0.7935 和 0.9732。面对同属非隧道阵营但具有连续浏览宏观复杂度的 TeraWFD_NOVPN，四个模型的基线迁移表现均呈现显著量级落差，Accuracy 数值最高的是 FS-Net 的 0.4334，Macro-F1 数值最高的是 AppScanner 的 0.3085。即使移除“是否经隧道转发”这一封装差异，传统公开任务上形成的高分印象仍无法直接外推到现代网站级连续浏览切片。这种跨域分数落差表明，样本内部的可分辨指纹会受到宏观时间窗、连续会话混合和页面生命周期内生后台流的显著稀释。

结合 `5.1` 的全量流归属原则，这种分数落差还暴露了 TeraWFD 中更具体的共享资源混淆（Shared-Resource Confusion）。由于第三章的操作性定义要求完整保留主页面触发的第三方请求，同一 CDN、广告联盟、分析脚本或跨站追踪器可能在不同目标网站的样本中继承不同网站标签。对传统基线模型而言，这意味着相似的浅层流量特征会被映射到多个目标标签；模型若主要依赖包数、方向突发、早期包字节或共享基础设施特征，就会在训练和测试中遭遇一对多的特征-标签关系。TeraWFD_NOVPN / TeraWFD_VPN 上的低分结果既来自页面内生后台流增多，也来自严格流级指纹保留测试下的共享第三方依赖干扰；默认参数机制在跨域宏观干扰下呈现出较高敏感性，现有 WFP 基线仍缺少从共享第三方依赖中剥离核心导航指纹的稳定表征空间。

区块 B 内也呈现类似的分区结果。ISCX-VPN 与 ISCX-Tor 上，AppScanner 和 YaTC 能给出 0.7752 / 0.7749、0.9564 / 0.9529、0.6775 / 0.8341、0.9820 / 0.6934 这类较高结果；面对同属隧道 / 混淆封装阵营但具有连续浏览宏观复杂度的 TeraWFD_VPN，FS-Net 在默认参数配置下维持了相对最高的 Accuracy（0.6816）和 Macro-F1（0.4462）。相近封装层级下，传统隧道化公开任务上的高分结论仍然无法直接转移为网站级连续浏览任务中的基线迁移表现。

在综合对照中，Trojan 隧道作为代表性的 TLS 封装隧道（Representative TLS-encapsulated Tunnel）进行单协议 case study。实验在直连 TLS/HTTPS 与 Trojan TLS 封装两类条件下均呈现出明显分数落差。连续浏览时间窗内，页面内生后台流和并行资源显著改变输入序列的特征分布，使基于单页面前提形成的特征表征难以稳定迁移到网站级连续浏览任务。两个封装切片上相近的结果差异表明，评测前提失配不是某一封装条件下的局部现象。

默认参数基线下的迁移表现差异也应回扣 `5.1` 的输入表示边界。YaTC 在 USTC-TFC、Cross-Platform 和 CIC-IoT-2022 上分别达到 0.9852、0.8423 和 0.9993 Accuracy，但在 TeraWFD_VPN 上 Macro-F1 只有 0.1234，说明其早期包字节图像机制高度依赖原有任务中的短时主导航对应关系；当宏观时间窗引入大尺度分布偏移后，该默认机制表现出较高敏感性。FS-Net 在默认参数配置下仍在 TeraWFD_VPN 上维持 0.6816 Accuracy 和 0.4462 Macro-F1，并在 TeraWFD_NOVPN 上维持 0.4334 Accuracy，表明带方向的长度时序特征在面对跨域宏观干扰时具备更宽泛的特征容错空间（Feature Tolerance Space），能够维持相对更优的判别下限。

Macro-F1 在区块内比较时尤其关键。在区块 A 中，CIC-IoT-2022 上 DF 的 Accuracy 为 0.7983，但 Macro-F1 只有 0.3251；TeraWFD_NOVPN 上 AppScanner 的 Accuracy 为 0.2812，Macro-F1 为 0.3085，FS-Net 的 Accuracy 为 0.4334，Macro-F1 仅为 0.2841。区块 B 中，ISCX-Tor 上 DF 的 Accuracy 达到 0.9227，但 Macro-F1 只有 0.5117；TeraWFD_VPN 上 YaTC 的 Accuracy 为 0.3986，Macro-F1 则为 0.1234。若只看 Accuracy，容易误判某些默认机制仍保持较强基线迁移表现；Macro-F1 则揭示出一旦任务被收紧到网站级连续浏览切片，类别层面的均衡识别能力会更早出现失衡。

公开数据集内部也已经出现 Acc-F1 断裂的早期征兆。ISCX-Tor 上，DF 的 Accuracy 达到 0.9227，但 Macro-F1 只有 0.5117；FS-Net 的 Accuracy 达到 0.9694，Macro-F1 也只有 0.5591。CIC-IoT-2022 上，DF 的 Accuracy 为 0.7983，Macro-F1 仅为 0.3251。类别层面的不均衡信号在小规模、较高信噪比或非网站级任务边界中常被总体 Accuracy 掩盖。TeraWFD 把这种裂缝放大到网站级连续浏览前提下，使其成为无法回避的主问题。

TeraWFD 子集上的 Acc-F1 断裂差值同样需要单独讨论。以 TeraWFD_NOVPN 为例，FS-Net 仍能保持 0.4334 的 Accuracy，但 Macro-F1 只有 0.2841；在 TeraWFD_VPN 上，YaTC 的 Accuracy 为 0.3986，而 Macro-F1 为 0.1234。这个现象说明默认机制出现了明显的标签逃逸：它们还能抓住少数具有强物理特征、强流量体量或更稳定内容组织的网站头部指纹，但对海量中长尾网站的判别下限已经显著下降。真实网站级流量指纹识别在宏观时间窗前提下呈现高度不均衡的类别区分度分布。TeraWFD 将这种过去被传统基准掩盖的结构性断层暴露出来，使“默认配置下的基线机制在大规模标签和页面内生后台流条件下是否仍能维持稳定判别下限”成为一个需要正面回答的问题。

这一组综合对照给出两条分区阅读的同构结论：非隧道公开任务上的高分结论无法直接外推到 TeraWFD_NOVPN，隧道 / 混淆封装公开任务上的高分结论也无法直接外推到 TeraWFD_VPN。公开任务结论的适用边界由样本单位、标签对象、封装协议前提和透明度前提共同限定。

### 5.4 标签规模扩展：宏观时间窗下的 Top-K 锚点匹配结果

Top-K 标签规模扩展实验只在 TeraWFD 内部进行，分别在 TeraWFD_NOVPN 与 TeraWFD_VPN 上构造前缀标签集合。这里所说的连续宏观时间窗，指单一目标站点或 Web 应用生命周期内的长时间持续访问。Top-K 任务被定义为连续宏观时间窗下的锚点特征提取与匹配：Top-50 完整包含 Top-10，Top-100 完整包含 Top-50，更大规模任务也沿用相同的前缀扩展方式，因此 K 值变化主要对应候选网站标签数量增加。在真实网页或 Web 应用生命周期中，由目标 URL、站内页面或其资源依赖触发的后台心跳、动态加载、第三方广告或 CDN 流量，均属于该网站标签下需要被模型处理的合法观测内容。对于任意轨迹流 `S_y`，模型的分类器始终被约束在当前任务定义的 `K` 个受控网站标签空间上，并输出这 `K` 个受控标签上的概率分布。为避免大词表带来的特征空间挤压干扰小规模评估，针对每一个 K 值任务，四种基准模型均在对应的 K 分类数据集上从头进行独立的训练与参数拟合。概率最大值对应的 Top-1 预测标签 `ŷ` 命中该轨迹流内主导航框架所属的网站标签 `y` 时，本文将其记为一次有效匹配。Accuracy 与 Macro-F1 衡量模型在页面内生后台流和共享资源干扰中剥离非目标特征，并对正确监控目标产生最高置信度响应的能力。本节同时将模型曲线与理论随机基线 $1/K$ 对照，观察不同输入表示在候选标签规模扩展下的有效保留率和 YaTC 的局部回弹。

Figure 2 与 Figure 3 展示了 TeraWFD_NOVPN 与 TeraWFD_VPN 在 Top-10 到 Top-1,000 范围内的锚点匹配结果变化。两张图的上半部分为 Accuracy，下半部分为 Macro-F1，灰色虚线给出随机猜测基线 $1/K$。

![Figure 2 TeraWFD_NOVPN 上的 Top-K 结果趋势](./assets/fig5-1_topk_novpn_research-writing-assistant_codex.svg)

![Figure 3 TeraWFD_VPN 上的 Top-K 结果趋势](./assets/fig5-2_topk_vpn_research-writing-assistant_codex.svg)

随机猜测基线从 Top-10 的 0.1000 降至 Top-1,000 的 0.0010，给出标签数量增长带来的理论参照。四个模型的曲线均高于该基线，说明它们仍保留可用的流量特征；有效比较应落在模型超过随机基线后的保留幅度和衰减速率上。以扣除随机基线后的有效保留率 $\Delta_K=(M_K-1/K)/(1-1/K)$ 衡量 Accuracy，DF 在 TeraWFD_NOVPN 上从 0.8588 降至 0.2483，在 TeraWFD_VPN 上从 0.8276 降至 0.3451；FS-Net 分别从 0.8662 降至 0.4123、从 0.9483 降至 0.7085。两类封装切片中，FS-Net 的保留率下降幅度均低于 DF，表明带方向的长度时序表示在当前 Top-K 设置下比纯方向序列保留了更多相对可分信号。Macro-F1 的变化与 Accuracy 基本一致，说明候选标签规模扩展同时改变总体匹配命中率和类别层面的平均识别能力。单看 Accuracy 会低估长尾类别和相似站点混淆对模型带来的压力。

这种相对保留率变化跨越两个网络场景。TeraWFD_NOVPN 的绝对分数整体更低，TeraWFD_VPN 在高 K 场景下也出现下降。两个子集改变了各模型的保留幅度和相对排序，却没有改变标签规模扩展会重排基线迁移表现这一共同结果。这意味着小规模标签空间上形成的模型排序依赖较弱的候选竞争条件。

从错误分布模式看，误判并非均匀分布于所有候选标签。Top-K 结果提供了一项经验性观察：候选集扩大后，不同类别承受的压力显著不同，模型排序和类别可分性会被候选池组成重排。共享 CDN、跨站追踪器或广告联盟等高频第三方依赖可能强化这种重叠竞争，并使部分误判持续推向相似基础设施标签；该机制需要依托 DOM、截图、TLS 会话密钥和访问元信息开展后续微观多模态归因。Top-K 扩展说明小规模候选集合下的基线迁移表现不稳定。

YaTC 的总体曲线不总是严格单调。TeraWFD_NOVPN 上，YaTC 从 Top-500 到 Top-1,000 的 Accuracy 由 0.3845 回升到 0.4366；TeraWFD_VPN 上，它从 Top-10 到 Top-50 的 Accuracy 由 0.8095 升至 0.9138。这个现象体现出 Top-K 规模扩展评测中的类别难度倾斜（Class Difficulty Skew）或构成假象（Compositional Artifact）：当长尾扩展引入的部分新网站具有极高区分度的结构特征，例如极端稳定的大文件加载、强体量突发或高度独特的资源组织时，这些易分类样本会在聚合指标上稀释核心混淆集合的下降，从而制造默认机制表现回升的测量假象。为避免总体曲线掩盖类别层面的重排，本文跟踪贯穿不同 K 值的固定类别。Table 9 选取若干代表性类别，展示同一类别从 Top-10 到 Top-1,000 的 F1 变化。

Table 9 YaTC 固定类别在 Top-K 锚点匹配扩展中的代表性 F1 变化

| 子集 | 类别 | Top-10 F1 | Top-1,000 F1 | 变化 |
|---|---|---:|---:|---:|
| TeraWFD_NOVPN | vk.com | 0.99 | 0.96 | -0.03 |
| TeraWFD_NOVPN | twitch.tv | 0.99 | 0.94 | -0.05 |
| TeraWFD_NOVPN | news.walla.co.il | 0.60 | 0.14 | -0.46 |
| TeraWFD_NOVPN | variety.com | 0.74 | 0.31 | -0.43 |
| TeraWFD_VPN | sport.es | 0.99 | 0.85 | -0.14 |
| TeraWFD_VPN | javatpoint.com | 1.00 | 0.80 | -0.20 |
| TeraWFD_VPN | rakuten.co.jp | 0.66 | 0.19 | -0.47 |
| TeraWFD_VPN | dailymail.co.uk | 0.99 | 0.55 | -0.44 |

固定类别结果说明，YaTC 的局部回弹揭示了显著的类别难度倾斜（Class-wise Difficulty Imbalance）。TeraWFD_NOVPN 上 vk.com 和 twitch.tv 到 Top-1,000 仍保持较高 F1，但 news.walla.co.il 和 variety.com 明显下滑；TeraWFD_VPN 上 sport.es 和 javatpoint.com 保留较多，而 rakuten.co.jp 和 dailymail.co.uk 的 F1 明显下降。Top-10 固定类别报告还给出一个重要边界：同一类别在 Top-10、Top-50、Top-100、Top-500 与 Top-1,000 中的 support 完全一致。因此，Table 9 中的 F1 分化源于候选标签集合扩展后类别间竞争关系和可分性结构重排。例如 TeraWFD_VPN 中 sport.es 的 support 为 4,184，Top-1,000 F1 仍有 0.85；rakuten.co.jp 的 support 为 4,255，却跌至 0.19。这一对照排除了样本数不足的单因子解释，说明真实连续时间窗内的网站指纹鲁棒性并不单纯取决于 support，而与网站自身资源并发结构、共享依赖形态和页面生命周期内生后台流密切相关。后续低开销混淆机制应优先抹平高辨识度站点暴露出的稳定突发、共享第三方依赖和长连接尾部特征，而不是对所有流量执行无差别扰动。具体网站的下降机制需要借助 TeraWFD 的 DOM、截图、TLS 会话密钥和访问元信息做微观多模态溯源。该问题将作为后续独立议题展开。类别难度倾斜使局部回弹成为测量假象，不能被解读为标签规模压力消失的证据。

Top-K 实验通过扣除随机基线后的有效保留率与固定类别 F1 指标，量化了不同输入表示在候选规模扩展下的鲁棒性分化趋势。小规模候选网站集合下得到的基线迁移表现，不能直接代表大规模网站级任务中的相对稳定性。

### 5.5 小结

本章的实证结果沿着同一条证据链展开。评测前提证伪实验首先表明：首页训练和全站单页面训练迁移到连续页面访问测试集时出现显著断层；训练、验证和测试同时采用连续会话样本时，验证集与测试集差距才显著收缩。这一结果直接表明单页面前提系统性抬高了目标场景下的性能估计。

按封装环境分区的综合对照随后表明：公开评测基准在特定任务边界下仍有参考价值，但这些结果需要在同一封装阵营内部阅读；对应到 TeraWFD_VPN 与 TeraWFD_NOVPN 所代表的网站级连续浏览任务时，不同模型呈现出明显跨域分数落差。模型输入表示也会在连续浏览宏观轨迹下被重新筛选，带方向的长度时序特征在当前结果中比纯方向序列和早期包字节图像保留了更多可分信号。

标签规模扩展最后表明：引入 $1/K$ 随机基线后，模型仍显著高于 chance level，但超过随机基线的有效保留率随 K 增大出现分化。FS-Net 在两个自建统计子集上的保留率下降幅度低于 DF，显示带方向的长度时序特征在当前设置下具有更宽的特征容错空间；YaTC 的局部非单调变化则源于固定类别层面的分化、核心混淆集压力和新增易分类类别稀释。固定类别 support 不变进一步排除了样本数差异这一解释，使类别难度倾斜成为更合理的读法。由此，旧评测前提下形成的默认参数基线表现，需要在连续页面访问、按封装环境对齐的公开对照，以及宏观时间窗下的网站级锚点匹配任务前提下重新校准；同时，Top-K 评测本身也需要报告随机基线、类别组成和先验难度，否则聚合指标会产生测量假象。
